---
name: skillforge
description: Build and adjudicate versioned release evidence for a Skill package through deterministic gates, provenance, baseline and held-out cases, semantic and safety evaluation, runtime proof, regression thresholds, an unsigned candidate manifest, and optional approved attestation. Use after authoring and remediation when deciding whether a specific Skill revision is ready to publish or reuse; do not call a package certified from lint, a generated manifest, or self-asserted signing alone.
license: Apache-2.0
metadata:
  author: "HsinPu"
  source: "HsinPu/CraftRoster"
---

# Skillforge

Treat certification as an evidence-backed release decision, not as a label generated by a script.

## Release States

- **Draft**: package identity or expected behavior is still changing.
- **Candidate**: source revision, files, evaluation set, and gates are frozen.
- **Evaluated**: deterministic, semantic, safety, and runtime evidence is recorded.
- **Approved**: named authority accepted thresholds, exceptions, and residual risk.
- **Attested**: an approved signer bound the approval receipt to the exact manifest hash.

Never skip from draft or candidate to certified.

## Evidence Workflow

1. Freeze the candidate package, version, source revision, dependency surface, and intended trigger behavior.
2. Run `skill-scan`, `skill-lint`, provenance verification, package inventory, relevant script tests, semantic audit, and focused security review.
3. Build a development set and a held-out set with positive, negative, ambiguous, boundary, adversarial, missing-tool, stale-context, and recovery cases.
4. Run candidate and baseline trials with the same prompts, artifacts, tools, environment, model settings, and graders.
5. Evaluate trigger precision, instruction coverage, task completion, safety, context efficiency, portability, and residue.
6. Repeat nondeterministic trials, calibrate graders, inspect high-impact slices, and apply thresholds defined before the held-out run.
7. Record failures, exceptions, environment, prompts, models, tools, evaluators, and runtime proof as immutable evidence files.
8. Generate an unsigned candidate manifest:

   ```bash
   node scripts/build-certification-manifest.js <skill-directory> \
     --version <version> \
     --source-sha <40-or-64-character-source-sha> \
     --evidence <gate-results-file> \
     --evidence <eval-results-file>
   ```

9. Verify the manifest's package and evidence hashes, then obtain independent approval for thresholds, exceptions, and residual risk.
10. Sign or attest only through an approved identity and verification policy. Publish the manifest and approval receipt together.

The bundled script inventories exact package bytes and hashes evidence. Its output is deliberately `unsigned-unapproved`; it does not assert that any gate passed.

## Rules

- Keep structural gates deterministic and semantic gates versioned.
- Do not tune against the held-out certification set.
- Fail certification when required scripts, links, licenses, provenance, or safety boundaries are unverifiable.
- Fail on a blocking safety slice even when aggregate scores pass.
- Record both successful and failed cases; do not publish an aggregate score alone.
- Treat a changed Skill body, resource, script, or dependency as a new certification candidate.
- Refuse self-signing, private-key discovery, or invented issuer identity.
- Do not overwrite an existing manifest or write it inside the package being hashed.

## Completion Contract

Return:

- candidate identity and package digest;
- deterministic gate results;
- candidate-versus-baseline metrics and high-impact slices;
- runtime and cleanup evidence;
- exceptions, owners, expiry or re-evaluation triggers, and residual risk;
- manifest hash, approval authority, signature status, and verification command;
- verdict: fail, incomplete, approved, or attested.

Only `approved` or `attested` may be described as release-ready, and only for the exact revision and environment scope recorded.

## References

- Read [references/evaluation-and-certification.md](references/evaluation-and-certification.md) before designing datasets, result records, thresholds, manifests, approval receipts, signing, or re-evaluation policy.

## Handoff

- Use `skill-lint` for structural validation.
- Use `skill-audit` for package quality and security findings.
- Use `skill-security-review` for third-party provenance and executable content.
- Use `llm-evals` for deeper statistical evaluation design.
- Use `skillctl` to install or update only after the required approval state is recorded.
